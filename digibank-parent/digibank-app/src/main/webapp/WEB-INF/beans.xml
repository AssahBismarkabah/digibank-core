<?xml version="1.0" encoding="UTF-8"?>
<!--
    Setting bean-discovery-mode="none" prevents WildFly's Weld CDI container from
    scanning the WAR for CDI beans.  Spring Boot uses its own IoC container; CDI
    discovery is not required and causes a conflict with the bundled
    hibernate-validator-cdi interceptor (WELD-001408).
-->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                           https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0"
       bean-discovery-mode="none">
</beans>
