= DigiBank Dynamic Security Validation Report
:doctype: article
:toc:
:sectanchors:

== Workshop Context

This report documents the runtime security validation required by
`work1-devsecops-practices.adoc`. Static analysis examines source code and
configuration before execution. DAST complements it by sending requests to a
running DigiBank system and evaluating the externally observable responses.

The system under test is the microservices implementation. The API Gateway is
the external entry point; customer, account, transaction and compliance
services remain internal Compose services.

== Test Environment

The validation uses Java 21, Spring Boot 4.1.0, Maven, Docker Compose and
PostgreSQL. The local gateway is available at `http://127.0.0.1:8080`.

Public endpoints are `/` and `/actuator/health`. Business endpoints require
HTTP Basic authentication at the gateway:

* `USER`: normal banking API access.
* `ADMIN`: normal banking access plus compliance and notification access.

Passwords are supplied through `GATEWAY_USER_PASSWORD` and
`GATEWAY_ADMIN_PASSWORD`. The repository defaults are for local education only.

== DAST Method

. Preserve a pre-remediation Newman and ZAP baseline.
. Build the application and images.
. Start the Compose stack and verify readiness.
. Replay functional and security collections with Newman.
. Run the OWASP ZAP baseline scan against the gateway.
. Analyze the externally visible findings.
. Apply gateway authentication, authorization and response hardening.
. Replay the same collections.
. Compare the baseline and remediated reports.

The reproducible command sequence is:

[source,shell]
----
make clean
make build
make image-build
make up
make smoke
make validate-stack
make newman-scan
make zap-scan
----

== Baseline Findings

The pre-remediation reports are stored under `dast/reports/baseline/`.

The baseline campaign is used to record the actual behavior before security
enforcement. Findings are categorized as functional behavior, security
behavior, scanner observations, or accepted educational limitations.


== Workshop 2 Static Analysis Continuation

Workshop 2 extends this security work from runtime observation to static analysis.
The purpose is to inspect the source code, configuration, dependencies and test
strength before the application is executed. This is the Shift Left step: risks
are identified close to the point where they are introduced, then corrected and
rechecked before the runtime validation from Workshop 1.

=== Static analysis scope

The current Spring Boot microservices implementation is the system under review.
The analysis covers the API Gateway, business services, platform services,
Maven dependencies, application configuration, Docker and Compose files, REST
controllers, DTO validation, exception handling, inter-service calls and tests
around security-sensitive behavior. The workshop document refers to the earlier
Java 17 modular-monolith version; this implementation uses the current Java 21
microservices parent and its eight Maven modules.

=== Reproducible commands

The repository exposes the static checks through the root Makefile:

[source,shell]
----
make build
make sast-dependency-check
make sast-pitest
make sast
make sast-sonar
----

`make sast-dependency-check` generates the OWASP Dependency-Check report.
`make sast-pitest` generates mutation-testing reports. `make sast-sonar` sends
the project to a reachable SonarQube server and requires a token. The combined
`make sast` command runs the local Dependency-Check and PITest controls.

=== Local SonarQube configuration

SonarQube is not required for ordinary local builds. It is required locally only
when a SonarQube server is running and a local dashboard is needed. In that case
set the values in the shell, not in a committed file:

[source,shell]
----
export SONAR_HOST_URL=http://localhost:9000
export SONAR_ORGANIZATION=<sonarcloud-organization>
export SONAR_PROJECT_KEY=<sonarcloud-project-key>
export SONAR_TOKEN=<local-sonarqube-token>
make sast-sonar
----

The token must remain outside Git. The GitHub Actions SonarCloud job supplies
`SONAR_HOST_URL`, `SONAR_ORGANIZATION`, `SONAR_PROJECT_KEY` and `SONAR_TOKEN`
through repository secrets and runs the same Maven analysis used locally.

=== Static-analysis evidence included

The findings in this continuation come from the completed scans and are
supported by the generated reports and captured screenshots:

* SonarQube issues, security hotspots and quality observations;
* Dependency-Check findings and their severity;
* PITest mutation score and surviving mutants;
* source locations reviewed before remediation;
* code or configuration changes made in response;
* tests and repeat scans demonstrating the result; and
* findings retained as false positives, accepted risks or educational limits.

Each completed finding has an observed result, an interpretation, a remediation
decision and a revalidation result. Remaining dependency findings and
surviving PITest mutants are explicitly recorded as follow-up work rather than
being presented as resolved.

=== SonarCloud baseline execution

The SonarCloud analysis was executed again after JaCoCo coverage generation.
All nine Maven projects reached `BUILD SUCCESS`, and SonarCloud accepted the
analysis for project `assahbismarkabah_digibank`.

The dashboard is available at:

https://sonarcloud.io/dashboard?id=assahbismarkabah_digibank

The analysis processed 74 source files across the microservices implementation.
The updated dashboard reports 33.2% overall coverage and 2.8% duplication.
JaCoCo XML reports were generated for the tested services. The notification
service has no test sources, so it does not produce execution data.

=== Workshop 2 implementation status

The following controls are configured and reproducible:

* Maven compilation and automated tests pass for all nine Maven projects;
* JaCoCo coverage report generation is enabled for tested modules;
* SonarCloud source analysis has completed successfully;
* OWASP Dependency-Check has a Make target and a CI report-upload path;
* PITest has a Make target and a CI report-upload path; and
* the existing GitHub Actions workflow preserves static-analysis reports even
  when a scan fails;
* the CI workflow caches the Dependency-Check NVD database between runs and
  checks for NVD updates at most once every seven days; and
* SonarCloud runs as a separate CI job with repository secrets and JaCoCo
  coverage generation.

Dependency-Check findings have now been reviewed. The initial API Gateway
baseline contained 43 unique CVEs; after the Spring Boot `4.1.1` upgrade and a
complete rerun, no unsuppressed CVEs remain. The Tomcat findings were removed by
the `11.0.25` patch override. Two remaining dependency matches were confirmed
as false positives and are covered by narrowly scoped, documented suppression
rules: Angus Activation was matched to a Jakarta Mail issue, and Hibernate
Validator was matched to the unrelated Nu Html Checker issue.

The initial NVD database update completed successfully. The first analysis was
then stopped by repeated `401 Unauthorized` responses from the optional
Sonatype OSS Index analyzer. The Dependency-Check command now disables that
optional analyzer and produces both HTML and JSON reports from the NVD data.
This is a scanner-integration limitation, not evidence of a dependency CVE.

The completed NVD-based report was generated for the reactor and service
modules. The initial API Gateway report contained 43 unique CVE identifiers:

* 15 Critical;
* 13 High;
* 14 Medium; and
* 1 Low.

Most findings are repeated transitively across the services and relate to the
shared Spring Framework, Spring Security, Log4j, PostgreSQL, Hibernate and
embedded Tomcat dependencies. The
report is therefore a dependency-risk baseline, not a conclusion that every
listed CVE is exploitable in DigiBank. Each item must be checked against the
affected feature and the dependency's fixed version before remediation or
suppression. The generated HTML and JSON reports are stored under each
service's `target/` directory and are uploaded by CI.

The first remediation decision is to upgrade the shared Spring Boot parent and
dependency-management import from `4.1.0` to the available patch release
`4.1.1`. This keeps the same Spring Boot major/minor line while allowing the
maintained dependency set to move forward. The build and Dependency-Check scan
must be rerun before any remaining finding is suppressed. Findings tied to
unused features, such as WebFlux, RSocket, WebAuthn, LDAP or Tomcat example
applications, will be documented as not applicable only after the dependency
tree and application configuration confirm that those features are absent.

After upgrading Spring Boot to `4.1.1`, overriding embedded Tomcat to `11.0.25`
and rerunning the complete reactor scan, no unsuppressed CVEs remain. The
generated reports still preserve the two reviewed false-positive matches under
the versioned suppression file. The scan completed successfully across all
nine Maven projects.

=== Workshop 2 completion status

The static-analysis setup is operational and reproducible. SonarCloud with
JaCoCo, PITest, Dependency-Check, Maven verification and CI report-upload
controls have all been executed. The justified code remediation identified by
SonarCloud was applied, and the shared Spring Boot patch upgrade reduced the
dependency baseline from 43 initial API Gateway CVEs to zero unsuppressed CVEs.

Workshop 2 is complete as a documented analysis and remediation exercise, with
the dependency scan now has zero unsuppressed CVEs. The two reviewed false
positives are explicitly scoped in `dependency-check-suppressions.xml`; no
finding is hidden by a severity threshold.

The PITest baseline has now been executed successfully after adding the PITest
JUnit 5 adapter required by the current test stack. All eligible Maven modules
completed mutation analysis and the reactor ended with `BUILD SUCCESS`.
Mutation results are module-specific: some modules killed all generated
mutants, while the gateway and business services also contain surviving or
uncovered mutants. Those results are test-strength observations to review, not
PITest execution failures. HTML reports are generated under each eligible
module's `target/pit-reports/index.html` directory.

=== SonarCloud finding decisions

The updated SonarCloud dashboard reports a passed quality gate, 33.2% overall
coverage, 2.8% duplication and 30 open issues. Two security issues are visible
in the security snapshot:

* The High CSRF finding is an intentional configuration decision for this
  workshop API. The gateway uses HTTP Basic authentication, has a stateless
  session policy and does not create browser sessions or authentication
  cookies. CSRF protection is designed for browser credentials automatically
  attached to stateful sessions; the Newman session-behavior checks verify the
  selected stateless model. This remains an accepted, documented limitation
  rather than an unreviewed finding.
* The Medium URL-construction finding is treated as a real issue. Gateway path
  variables are now encoded with `UriUtils.encodePathSegment` before they are
  forwarded to downstream services. This prevents path-variable contents from
  changing the downstream request path while preserving the existing API
  routes.

The remaining pasted findings are informational timestamp observations. They
identify uses of `LocalDateTime.now()` in account and compliance error models
and in the gateway exception handler. They do not expose secrets, alter
authorization, or create an injection path. They are retained as a consistency
and maintainability follow-up: if timestamps become part of a cross-service
contract, they should be standardized on an explicit UTC `Clock` or `Instant`.
They are not treated as Workshop 2 security remediations.

The SonarCloud screenshots in
`docs/devsecops/presentation/assets/screenshots/` provide the dashboard and
open-issues evidence. The URL-construction finding was remediated with encoded
gateway path segments and the updated analysis was completed successfully.
|===
| Finding | Runtime observation | Risk | Evidence
| B-01 | Business API routes were reachable without gateway authentication. | Any caller could reach exposed operations. | Baseline Newman report
| B-02 | No role distinction existed at the gateway. | Sensitive compliance operations had no gateway authorization boundary. | Baseline API behavior
| B-03 | Some not-found/error paths could expose business details. | External callers could learn implementation or resource information. | Newman response assertions
| B-04 | The gateway exposed only a partial security-header baseline. | Browser and proxy protections were incomplete. | Baseline ZAP report
| B-05 | Sensitive API responses did not explicitly disable caching. | Shared caches could retain user-specific responses. | Baseline ZAP report
|===

== Remediation

The API Gateway now uses stateless Spring Security HTTP Basic authentication.
Business routes require `USER` or `ADMIN`; compliance and notification routes
require `ADMIN`. Health and root endpoints remain public for orchestration and
readiness checks.

The gateway also adds `X-Content-Type-Options`, `X-Frame-Options`,
`Cross-Origin-Resource-Policy`, and no-store cache directives for API
responses. Existing generic error handling and malformed-request handling are
retained and exercised through the security collection.

The authentication model is intentionally suitable for a local workshop. It
does not represent a production identity provider, OAuth2 deployment, or TLS
termination strategy.

== Revalidation

`DigiBank-DAST-Validation.postman_collection.json` verifies authenticated
functional regression. `DigiBank-DAST-Security.postman_collection.json` covers
the eight workshop categories:

* authentication
* authorization
* functional regression
* input validation
* error handling
* information exposure
* session behavior
* remediation verification

The security checks verify that missing credentials return `401`, insufficient
roles return `403`, valid roles are accepted, public health endpoints remain
available, malformed input does not disclose internals, expected headers are
present, and no session cookie is created.

The Newman runner writes separate functional and security JSON/HTML reports in
`dast/reports/<timestamp>/`. It exits non-zero if either collection fails.

The latest local replay completed successfully:

* Functional collection: 24 requests, 74 assertions, 0 failures.
* Security collection: 12 requests, 19 assertions, 0 failures.
* Authentication: missing and invalid credentials returned `401`.
* Authorization: a `USER` request to compliance returned `403`; an `ADMIN`
  request was accepted.
* Input and error handling: malformed JSON and unknown resources returned
  controlled `4xx` responses without stack traces or internal class names.
* Session behavior: authenticated requests did not create a session cookie.

The ZAP report is generated separately by `make zap-scan`. ZAP findings must be
compared with the baseline rather than treated as an automatic claim that the
application has no security risk.

The latest local ZAP baseline reported zero High alerts, one Medium alert, zero
Low alerts and one Informational alert. The Medium alert is the expected local
HTTP Basic Auth over plain HTTP warning; production deployment must use TLS.
The Informational alert concerns cacheability of the public root and static
resource responses, not authenticated banking API responses, which receive
`Cache-Control: no-store, private`.

== Functional Regression

Security enforcement must not break normal DigiBank behavior. The validation
therefore retains checks for customer, account, transaction and compliance
flows, gateway routing, public health checks, and complete Compose readiness.

== CI Evidence

The GitHub Actions workflow executes build and tests first, then image build,
Compose startup, smoke checks and stack validation. The Newman job runs after
that readiness gate. The ZAP job runs after Newman. Newman and ZAP reports are
uploaded as independent artifacts, including when a job fails.

Newman authentication and authorization failures are blocking. ZAP low-risk
and informational findings remain visible in artifacts while they are reviewed
and assigned an explicit remediation decision.

== Accepted Risks And Limitations

* The workshop uses a single-host Docker Compose deployment.
* HTTP Basic Auth is a local educational security model.
* Authentication is stateless and does not create server sessions.
* There is no production identity provider or OAuth2/OIDC integration.
* Local validation does not provide production TLS termination.
* Default local passwords must be replaced through environment variables in a
  real deployment.
* ZAP reports a Medium local-only warning because HTTP Basic Auth is exercised
  over plain HTTP; this is not acceptable for production traffic and is why the
  workshop scope is explicitly local.
* Informational cacheability observations for public static resources remain
  visible in the report and are not treated as sensitive API data.

== Workshop Objective Mapping

  [cols="1,4", options="header"]
|===
| Objective | Evidence
| A3.1 | This report's SAST/DAST explanation and runtime method.
| A3.2 | Versioned functional and security Postman collections.
| A3.3 | Newman assertions and captured HTTP response behavior.
| A3.4 | Basic authentication, role checks and stateless-session checks.
| A3.5 | Baseline Newman observations and ZAP reports.
| A3.6 | Gateway security configuration, headers, cache and error hardening.
| A3.7 | Post-remediation Newman and ZAP replay.
| A3.8 | This report, versioned reports, CI artifacts and reproducible commands.
|===
